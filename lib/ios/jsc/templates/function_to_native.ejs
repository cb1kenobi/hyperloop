
@import JavaScriptCore;
<% frameworks.forEach(function(f){ -%>
@import <%=f%>;
<% }) -%>

// export methods we use
<% externs.forEach(function(e) { -%>
<%- e %>
<% }) -%>

JSValueRef Hyperloop<%=object.mangledName%> (JSContextRef ctx, JSObjectRef function, JSObjectRef object, size_t argumentCount, const JSValueRef arguments[], JSValueRef* exception)
{
	 <%- indentify(fnbody, '    ') %>
}
