if (!handled && argumentCount >= 1) {
	handled = true;
	<%- binding.className %>^ arg$<%- index %>$0 = HyperloopJSValueRefTo<%- binding.className %>(ctx, arguments[0], exception, 0);
	// TODO: If we could get the method reference from arguments[1], that would help us remove the @class "forHandler" property.
	auto arg$<%- index %>$1 = &<%- binding.className %>::<%- binding.methodName %>;
	instance = <%= object.is_enum ? '' : 'ref ' %>new <%- fullInstanceName %>(arg$<%- index %>$0, arg$<%- index %>$1);
}