<%- renderTemplate('templates/doc.ejs') %>

#pragma once
using namespace Platform;
#include <string>
#include <JavaScriptCore/JavaScript.h>
#include "hyperloop.h"
#include "JSBuffer.h"

<% files.headers.sort().forEach(function(i) { -%>
#include "<%- i %>"
<% }) -%>

class <%=gen.prefix%><%=gen.name%>
{
public:
	static void loadWithObject(JSContextRef ctx, JSObjectRef object);
	static JSStringRef source();
};

<%-bodyHeader%>