JSValueRef HyperloopCastTo<%- mangledName %>(JSContextRef ctx, JSObjectRef function, JSObjectRef object, size_t argumentCount, const JSValueRef arguments[], JSValueRef* exception)
{
	if (argumentCount != 1) {
		HyperloopRaiseNativeToJSException(ctx, exception, ref new Exception(-1, "Incorrect number of arguments passed when casting to <%- mangledName %>!"), __FILE__, __FUNCTION__, __LINE__);
		return JSValueMakeUndefined(ctx);
	}
	auto instance = HyperloopJSValueRefTo<%- mangledName %>(ctx, arguments[0], exception, 0);
	return Hyperloop<%- mangledName %>ToJSValueRef(ctx, instance);
}